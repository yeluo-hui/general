import AnswerKeyDriver from "@/components/rdtable/exam/answer-key-driver"

export default class ExamTreeDriver extends AnswerKeyDriver {
  onInit (exam ) {
    super.onInit(exam)
  }


  changeKey(question) {
    super.changeKey(question)
  }
}
